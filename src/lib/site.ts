// Single source of truth for the canonical site URL. Matches the
// metadataBase in src/app/layout.tsx. Used by sitemap, JSON-LD, OG image
// builder, and metadata helpers. Do NOT introduce a separate
// NEXT_PUBLIC_SITE_URL env var.
export const SITE_URL = "https://californiafastener.com";
